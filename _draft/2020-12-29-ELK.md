---
layout : post
title : Elastic + Kibana + Open Distro Alert 설치 on Ubutu(arm64)
tags : tip
typora-root-url: ..
---

Ubuntu (arm64) 배포버전에 Elasticsearch + Kibana 를 설치를 위한 몇가지 준비 사항이 필요합니다.

[Elasticsearch 설치 가이드](https://www.elastic.co/guide/en/elasticsearch/reference/7.10/deb.html#deb-repo) 를 참조 하면 좋습니다.



```

```







# Kibana 설치 하기

Open Distro Alert plugin 을 설치를 위해선 지원하는 버전 설치가 중요합니다.

https://opendistro.github.io/for-elasticsearch-docs/docs/kibana/plugins/ 의 플러그인 버전과 지원하는 Kibana 설지 버전을 확인을 먼저 하시기 바랍니다.

문서를 작성하는 시점에는 deb 패키지로 배포판이 준비 되지 않아 홈페이지의 `DEB ARRCH64 ` 를 이용하여 수동 설치방법으로 진행합니다.



## 준비하기



https://artifacts.elastic.co/downloads/elasticsearch/elasticsearch-7.10.0-linux-aarch64.tar.gz

```
wget https://artifacts.elastic.co/downloads/elasticsearch/elasticsearch-7.10.0-linux-aarch64.tar.gz

tar xvzf elasticsearch-7.10.0-linux-aarch64.tar.gz
sudo mv kibana-7.10.0-linux-aarch64 /usr/share/kibana
sudo mv /usr/share/kibana/confi /etc/kibana


```



## kibana 유저 및 그룹 생성

```
sudo groupadd --gid 120 kibana
sudo useradd --uid 120 --gid 120 --home-dir /usr/share/kibana --no-create-home kibana
```



## Open Distro Alert plugin 설치

```
cd /usr/share/kibana
sudo bin/kibana-plugin install https://d3g5vo6xdbdb9a.cloudfront.net/downloads/kibana-plugins/opendistro-alerting/opendistroAlertingKibana-1.12.0.2.zip
```



## 설정 파일 생성

/etc/kibana/kibana.yml 을 아래와 같이 수정합니다.

```yaml
server.host: "0.0.0.0"
elasticsearch.hosts: ["http://127.0.0.1:9200"]
kibana.index: ".kibana"

```



## 서비스 등록 및 실행

서비스 등록을 위한 준비가 필요합니다. 

### /etc/init.d/kibana

서비스 등록을 위한 스크립트 파일을 만들어야 합니다.

vi /etc/init.d/kibana

```bash
#!/bin/sh

# Init script for kibana
# Maintained by
# Generated by pleaserun.
# Implemented based on LSB Core 3.1:
#   * Sections: 20.2, 20.3
#
### BEGIN INIT INFO
# Provides:          kibana
# Required-Start:    $remote_fs $syslog
# Required-Stop:     $remote_fs $syslog
# Default-Start:     2 3 4 5
# Default-Stop:      0 1 6
# Short-Description:
# Description:       Kibana
### END INIT INFO

name=kibana
program=/usr/share/kibana/bin/kibana
pidfile="/var/run/kibana/$name.pid"

[ -r /etc/default/$name ] && . /etc/default/$name
[ -r /etc/sysconfig/$name ] && . /etc/sysconfig/$name

export KIBANA_PATH_CONF
export NODE_OPTIONS

[ -z "$nice" ] && nice=0

trace() {
  logger -t "/etc/init.d/kibana" "$@"
}

emit() {
  trace "$@"
  echo "$@"
}
start() {
  [ ! -d "/var/log/kibana/" ] && mkdir "/var/log/kibana/"
  chown "$user":"$group" "/var/log/kibana/"
  chmod 2750 "/var/log/kibana/"

  [ ! -d "/var/run/kibana/" ] && mkdir "/var/run/kibana/"
  chown "$user":"$group" "/var/run/kibana/"
  chmod 755 "/var/run/kibana/"

  chroot --userspec "$user":"$group" "$chroot" sh -c "

    cd \"$chdir\"
    exec \"$program\"
  " >> /var/log/kibana/kibana.stdout 2>> /var/log/kibana/kibana.stderr &

  # Generate the pidfile from here. If we instead made the forked process
  # generate it there will be a race condition between the pidfile writing
  # and a process possibly asking for status.
  echo $! > $pidfile

  emit "$name started"
  return 0
}
stop() {
  # Try a few times to kill TERM the program
  if status ; then
    pid=$(cat "$pidfile")
    trace "Killing $name (pid $pid) with SIGTERM"
    kill -TERM $pid
    # Wait for it to exit.
    for i in 1 2 3 4 5 ; do
      trace "Waiting $name (pid $pid) to die..."
      status || break
      sleep 1
    done
    if status ; then
      if [ "$KILL_ON_STOP_TIMEOUT" -eq 1 ] ; then
        trace "Timeout reached. Killing $name (pid $pid) with SIGKILL.  This may result in data loss."
        kill -KILL $pid
        emit "$name killed with SIGKILL."
      else
        emit "$name stop failed; still running."
      fi
    else
      emit "$name stopped."
    fi
  fi
}
status() {
  if [ -f "$pidfile" ] ; then
    pid=$(cat "$pidfile")
    if ps -p $pid > /dev/null 2> /dev/null ; then
      # process by this pid is running.
      # It may not be our pid, but that's what you get with just pidfiles.
      # TODO(sissel): Check if this process seems to be the same as the one we
      # expect. It'd be nice to use flock here, but flock uses fork, not exec,
      # so it makes it quite awkward to use in this case.
      return 0
    else
      return 2 # program is dead but pid file exists
    fi
  else
    return 3 # program is not running
  fi
}

force_stop() {
  if status ; then
    stop
    status && kill -KILL $(cat "$pidfile")
  fi
}
case "$1" in
  force-start|start|stop|force-stop|restart)
    trace "Attempting '$1' on kibana"
    ;;
esac

case "$1" in
  force-start)
    PRESTART=no
    exec "$0" start
    ;;
  start)
    status
    code=$?
    if [ $code -eq 0 ]; then
      emit "$name is already running"
      exit $code
    else
      start
      exit $?
    fi
    ;;
  stop) stop ;;
  force-stop) force_stop ;;
  status)
    status
    code=$?
    if [ $code -eq 0 ] ; then
      emit "$name is running"
    else
      emit "$name is not running"
    fi
    exit $code
    ;;
  restart)

    stop && start
    ;;
  *)
    echo "Usage: $SCRIPTNAME {start|force-start|stop|force-start|force-stop|status|restart}" >&2
    exit 3
  ;;
esac

exit $?

```



 vi /etc/default/kibana

```
user="kibana"
group="kibana"
chroot="/"
chdir="/"
nice=""


# If this is set to 1, then when `stop` is called, if the process has
# not exited within a reasonable time, SIGKILL will be sent next.
# The default behavior is to simply log a message "program stop failed; still running"
KILL_ON_STOP_TIMEOUT=0

BABEL_CACHE_PATH="/var/lib/kibana/optimize/.babel_register_cache.json"

KIBANA_PATH_CONF="/etc/kibana"

```



서비스 등록을 위한 해서 실행 권한을 주고 서비스 등록을 합니다

```
$chmod +x /etc/init.d/kibana
$sudo update-rc.d kibana defaults 95 10

```



## 실행

```
$sudo -i service kibana start
```

