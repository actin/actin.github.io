theme: jekyll-theme-hacker
url: "https://actin.github.io"
ls: false

exclude: [LICENSE, CNAME, README.md, .gitignore, Gemfile, Gemfile.lock]
permalink: pretty

defaults:
  - scope:
      path: "img"
    values:
    image: true
